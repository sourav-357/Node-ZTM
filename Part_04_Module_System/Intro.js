// The process.argv working example ---------------------------------------------------//

/*
const mission = process.argv[2];

if (mission === 'learn') {
    console.log('Time to write some Node code!');
} else {
    console.log(`Is ${mission} really more fun?`);
}
*/

// The example of asysnchronous function --------------------------------------------//

console.log("rabbit finished!");
console.log("Tortoise finished!");

// The example of asysnchronous function --------------------------------------------//

setTimeout(() => console.log('rabbit finished'), 1000);
setTimeout(() => console.log('Tortoise finished'), 500);

